## 1. Behavior Tests

- [x] 1.1 将 E2E 的 busy restart 现状断言改为“新会话取消旧请求并允许重新框选”，增加框选中重复启动场景；在当前实现上运行并记录预期失败，确保失败源于会话行为而非环境。
- [x] 1.2 为截图/裁剪、告知状态/确认以及 OCR 成功/失败建立可控延迟测试入口，覆盖跨标签页和连续启动；验证测试可按顺序释放回调，无真实 API 请求或生产调试接口。

## 2. Session Lifecycle

- [x] 2.1 在 `content.js` 中统一会话结束操作，复用于再次启动、主动取消与卸载，移除忙碌时忽略启动的分支；验证旧请求中止、新空白框选可用、重复清理幂等及标签页 B 不受 A 影响。
- [x] 2.2 为上传告知保留可结算句柄，在首次 await 前捕获确认所属会话及选区；验证告知读取中、弹窗打开时和已同意写入等待时替换均不上传旧选区，不将替换当成用户同意。
- [x] 2.3 在截图、裁剪、识别的成功/错误/最终清理路径及会话相关计时器中校验身份；验证旧成功不覆盖结果、旧异常不隐藏新进度、旧 finally 不结束新会话，主动取消后可立即重新启动。

## 3. Verification and Handoff

- [x] 3.1 运行完整竞态场景并在临时副本执行变异验证：移除会话保护后对应测试必须失败；记录原版通过和变异版失败，确认工作区运行源码未残留变异。
- [x] 3.2 运行 `npm run check`、`npm run package`、`openspec validate --all --strict --no-interactive`，检查所有规格场景及取消前后历史语义；把命令结果和场景映射写入本变更 `verification.md`。
- [x] 3.3 更新原重构的 `verification.md` 和导航以引用本行为变更的实际结果，逐项核对其任务 1.4 的场景及变异证据后才勾选，并重新确认阶段 1 门禁；主规格同步和归档按后续流程执行，不以规划完成代替实施验收。
