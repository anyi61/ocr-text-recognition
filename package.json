{
  "name": "ocr-text-recognition-extension",
  "version": "1.1.0",
  "private": true,
  "description": "Automated checks for the OCR Text Recognition Chrome extension",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:e2e": "node scripts/run-e2e.mjs",
    "check": "npm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
